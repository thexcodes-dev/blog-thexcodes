query GetPostsBySession ($slug: [String], $first: Int, $skip: Int) {
  posts(where: {sessions_some: {slug_in: $slug}}, orderBy: availableAt_DESC, first: $first, skip: $skip) {
    title
    slug
    description
    teachers { 
      name
    }
    image {
      url
    }
  }
}
