query GetPosts($first: Int, $skip: Int) {
  posts(orderBy: availableAt_DESC, first: $first, skip: $skip) {
    title
    slug
    description
    teacher {
      name
    }
    image {
      url
    }
    sessions {
      title
      color
      bgColor
    }
  }
}