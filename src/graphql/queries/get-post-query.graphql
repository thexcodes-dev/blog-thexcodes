query GetPost($slug: String) {
  post(where: {slug: $slug}) {
    text {
      html
    }
    image {
      url
    }
    slug
    title
    description
    sessions {
      title
      slug
    }
    teachers {
      name
    }
  }
}