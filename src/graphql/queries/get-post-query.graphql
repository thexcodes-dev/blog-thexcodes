query GetPost($slug: String) {
  post(where: {slug: $slug}) {
    text {
      html
    }
    imageUrl
    imageLabel
    slug
    title
    description
    views
    sessions {
      title
      slug
      color
      bgColor
    }
    teacher {
      name
    }
  }
}